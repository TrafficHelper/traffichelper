# TrafficHelper

This is the repo for our CS project. Project format:

- `backend/`
  - this folder contains the backend code written in ocaml
- `TrafficHelper/` (frontend)
  - this folder contains the frontend code in JS, for both web and non-web
